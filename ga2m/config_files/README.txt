In this folder is stored all the configurations of models tried. Below is the template for a configuration file with explanation of each argument.

#####
subset_features = feature1, feature2, etc.
num_interaction = integer
#####

subset_features: a comma separated list of features or "None" if all features are to be used.
num_interaction: The number of pair wise interactions for ga2m to search for.
